#!/usr/bin/env sh
#MISE description="run formatting and lint checks"
#MISE alias=["l"]

bin/lint/check/markdown &&
bin/lint/check/yaml &&
bin/lint/check/toml &&
bin/lint/check/shell &&
bin/lint/check/rust

if command -v editorconfig-checker >/dev/null 2>&1; then
  editorconfig-checker
fi
